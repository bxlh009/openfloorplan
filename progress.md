# 进度日志

## 会话：2026-08-07

### v0.3.0：可靠内核与六套本地装修风格
- **状态：** complete
- 完成 JSON v2 与 v1 迁移、显式事务撤销/重做、门窗真实开洞和示例一致性。
- 完成现代、北欧、日式、侘寂、工业、美式六套本地风格，联动 2D、墙地面、家具、天空和灯光。
- 完成自动取景和俯视透视，最终浏览器截图可辨认家具、墙体、地面和开口。
- 下一步进入交互与视觉阶段：属性面板、移动端、家具搜索分类和空错状态。

### v0.3.1：截图反馈修复
- **状态：** complete
- 依据用户实测图复现并修复近墙遮挡、门扇语义不一致、窗宽翻倍和地板外溢。
- 新增默认剖切视图和完整外观切换；新增两个纯逻辑回归测试并完成两种 3D 模式视觉验收。

### 阶段 6：可编辑设计闭环
- **状态：** in_progress
- 用户确认当前选择、标注、3D编辑和风格系统均未达到可用标准。
- 本轮优先完成可验证的编辑闭环，再处理构件造型，避免继续用配色冒充风格。
- 已定位家具点击被墙体抢占：调整为家具/门窗优先命中，并增加蓝色轮廓、控制点和“已选择”名称。
- 已把标注起点放入共享状态，绘制层可在首次点击前和两次点击之间实时显示操作提示与长度。
- 已加入建筑构件风格数据合同和门窗沿墙安全定位函数，核心测试增至 10/10。
- 已实现 3D 射线拾取、地面拖动、2D 实时同步、蓝色选框和单次撤销事务。
- 门窗属性已改为宽/高/离地/开启角/沿墙位置，移除会让构件脱墙的裸 X/Y 输入。
- 室内与构件风格已拆分；六类构件预设改变门板、窗格、框宽、踢脚线和顶线，六套室内预设开始改变沙发、床和桌的几何比例。
- 旋转家具命中已纳入纯逻辑回归，当前核心测试 11/11。
- 浏览器确认：标注首次点击前显示“设置起点”，首次点击后显示实时长度和“设置终点”；2D 选家具显示蓝框、控制点、名称与对象 ID。
- 浏览器确认：3D 点击选中餐桌并显示同一 ID，拖动后切回 2D 可见蓝色餐桌选框已移动；日式构件风格切换后顶线和构件造型可见改变。
- 浏览器门窗点击夹具未成功落墙，未把该项伪报为 E2E 通过；沿墙位置与边界由纯逻辑测试覆盖，属性渲染由语法和代码路径检查覆盖。
- 最终 v0.4.0 构建再次通过 11/11 测试、全部一方脚本语法检查、`git diff --check`，未残留 DEBUG 标记。
- 最终浏览器保留现代室内风格 + 日式建筑构件风格的 3D 剖切示例，构件选择状态与控制栏正常。

### v0.4.1：3D 家具双击选择
- **状态：** complete
- 3D 家具改为双击选中；普通单击不改变选择，已选家具仍可直接拖动并同步项目数据。
- 选中后属性区明确显示家具名称和对象 ID，状态栏提示“拖动家具移动，双击其他家具切换选择”。
- 浏览器实测：一次单击后属性区仍为未选择；双击沙发后显示“已选择 / 沙发 / obj_1”和拖动提示。
- 最终通过 11/11 核心测试、全部一方 JavaScript 语法检查和 `git diff --check`。

## 测试结果
| 测试 | 预期结果 | 实际结果 | 状态 |
|------|---------|---------|------|
| JavaScript 语法检查 | 全部通过 | 全部通过 | PASS |
| Python 服务脚本编译 | 通过 | 通过 | PASS |
| 本地页面启动 | 页面和工具栏可见 | 页面正常加载 | PASS |
| 核心行为测试 | 迁移、风格、校验、历史、回载、开洞、剖切、门轴通过 | 9/9 通过 | PASS |
| JavaScript 语法检查 | 全部一方脚本通过 | 全部通过 | PASS |
| 差异检查 | 无空白错误 | `git diff --check` 通过 | PASS |
| 浏览器验收 | 六风格、载入、撤销、3D 和视觉正常 | 全部通过 | PASS |

## 错误日志
| 时间 | 错误 | 尝试次数 | 解决方案 |
|------|------|---------|---------|
| 2026-08-07 | file URL 被浏览器安全策略阻止 | 1 | 使用短时 localhost 预览 |
| 2026-08-07 | ui.js 批量补丁上下文不匹配 | 1 | 拆成小块补丁，按当前函数边界逐段修改 |
| 2026-08-07 | PowerShell 搜索命令引号未闭合 | 1 | 分离搜索与语法检查，避免嵌套转义 |
| 2026-08-07 | 已释放的旧预览标签无法重新 claim | 1 | 从现有浏览器绑定创建新标签，不重新选择浏览器 |
| 2026-08-07 | 新标签仍显示旧版缓存页面 | 1 | 使用版本查询参数重新导航并核对 DOM |
| 2026-08-07 | 浏览器隔离执行环境无法直接读取页面顶层 const State | 1 | 通过可见 DOM 状态和公开界面验证，不依赖内部变量 |
| 2026-08-07 | 新 HTML 搭配了缓存中的旧 ui.js，风格按钮无监听器 | 1 | 为全部一方脚本增加统一版本查询参数后重新加载 |
| 2026-08-07 | 风格选择器把 body 误当作风格按钮并写入 aria-pressed | 1 | 选择器限定为 .style-card，body 使用独立 data-interior-style |
| 2026-08-07 | 文件选择后 body locator 焦点已变化，快捷键 press 失败 | 1 | 改用标签级键盘输入并通过可见风格状态验收撤销 |
| 2026-08-07 | 新风格按钮没有加载新版 CSS | 1 | 为 style.css 增加与脚本一致的版本参数 |
| 2026-08-07 | 3D 示例被 200 米地板与过远相机压缩成小点 | 1 | 地板边距改为 2 米，并在载入后按户型边界自动取景 |
| 2026-08-07 | 自动取景后近侧墙仍遮挡室内家具 | 1 | 默认镜头改为更高俯角，示例补充门窗并重新视觉验收 |
| 2026-08-07 | 用户实测图显示墙体遮挡、门扇不一致、窗宽翻倍和地板外溢 | 1 | 增加镜头感知剖切模式，统一门轴模型，修正宽度与地板边界 |
| 2026-08-07 | 规划技能目录未提供 session-catchup.py | 1 | 直接读取 task_plan、progress、findings 并检查当前差异 |
| 2026-08-07 | Windows 下 `rg js/*.js` 不接受该 glob | 1 | 改为搜索 `js` 目录并排除 vendor 输出 |
| 2026-08-07 | 浏览器 locator 不提供 hover/boundingBox，隔离环境也无 PointerEvent/MouseEvent 构造器 | 3 | 改用浏览器原生指针移动验证标注预览 |
| 2026-08-07 | 3D drag 首次参数格式错误 | 1 | 使用要求的 path 点序列完成拖动 |
| 2026-08-07 | 外层 Statsig 遥测连续超时导致一次门窗验收耗时异常 | 1 | 停止猜坐标式长流程，保留已完成的本地浏览器证据与确定性测试 |
| 2026-08-07 | v0.4.1 多文件大补丁的 hunk 格式错误 | 1 | 拆分为独立小补丁并逐项复核差异 |
| 2026-08-07 | 浏览器隔离 DOM 代理不可扩展，无法挂临时事件计数器 | 1 | 改为可重复的真实单击/双击操作并读取可见属性状态 |
| 2026-08-07 | 刷新后仍加载旧版 view3d.js 查询参数 | 1 | 更新全部本地资源缓存参数并核对实际脚本 URL |

## 五问重启检查
| 问题 | 答案 |
|------|------|
| 我在哪里？ | v0.4.1 3D 双击选择完成 |
| 我要去哪里？ | 固定浏览器夹具、移动端、家具搜索分类和高质量本地 GLB 资产 |
| 目标是什么？ | 本地优先且可靠的户型与装修风格设计器 |
| 我学到了什么？ | 见 findings.md |
| 我做了什么？ | 完成可靠内核、六套风格、编辑闭环和 3D 双击选择验收 |
